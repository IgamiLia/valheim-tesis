<svg style="position:absolute;width:0;height:0;overflow:hidden">
  <defs>
    <filter id="noise-distort">
      <feTurbulence
        type="fractalNoise"
        baseFrequency="0.04 0.07"
        numOctaves="4"
        seed="8"
        result="noise"></feTurbulence>
      <feDisplacementMap
        in="SourceGraphic"
        in2="noise"
        scale="12"
        xChannelSelector="R"
        yChannelSelector="G"></feDisplacementMap>
    </filter>
  </defs>
</svg>
<div class="section-transition" aria-hidden="true">
  <span class="transition-rune">✦ ᛟ ✦</span>
</div>

<style>
  .section-transition {
    position: relative;
    width: 100vw;
    height: 200px;
    align-self: stretch;
    background: #000;
    margin: -2px 0;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: visible;
  }

  .section-transition::before,
  .section-transition::after {
    content: "";
    position: absolute;
    width: 100%;
    height: 150px;
    filter: url(#noise-distort);
  }

  /* Borde superior irregular */
  .section-transition::before {
    top: -100px;
    background: linear-gradient(to bottom, transparent 0%, #000 60%);
  }

  /* Borde inferior irregular*/
  .section-transition::after {
    bottom: -100px;
    background: linear-gradient(to top, transparent 0%, #000 60%);
  }

  /* Línea dorada central */
  .transition-rune {
    color: transparent;
    background: linear-gradient(180deg, #daaa29 35%, #f5ca7b 55%, #daaa29 65%);
    -webkit-background-clip: text;
    background-clip: text;
    font-family: Georgia, serif;
    font-size: 2rem;
    letter-spacing: 1rem;
    opacity: 0.6;
    text-align: center;
    width: 100%;
    position: relative;
    z-index: 2;
    text-shadow:
      0 0 12px rgb(212, 184, 57),
      0 0 24px rgba(201, 162, 39, 0.3);
  }
</style>
